{
    "name": "onekey-docs",
    "version": "1.0.0",
    "scripts": {
      "dev": "mintlify dev",
      "build": "echo 'Static files ready' && mkdir -p dist && cp -r . dist/",
      "start": "mintlify dev"
    },
    "devDependencies": {
      "mintlify": "^4.0.0"
    }
  }